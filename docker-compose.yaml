version: "3.2"

services:
  loadbalance:
    image: nginx
    ports:
      - 3000:80
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf
      - ./config/conf.d/default.conf:/etc/nginx/conf.d/default.conf
